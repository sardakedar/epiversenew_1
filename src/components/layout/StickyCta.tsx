import ContactButton from '@/components/ui/ContactButton';

export default function StickyCta() {
  return (
    <div id="sticky-cta">
      <ContactButton className="btn btn-gold">Book a free demo →</ContactButton>
    </div>
  );
}
